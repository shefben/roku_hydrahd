<?xml version="1.0" encoding="utf-8" ?>
<component name="HomeView" extends="Group">
    <interface>
        <field id="args" type="assocarray" />
        <field id="requestNav" type="assocarray" alwaysNotify="true" />
        <field id="loading" type="bool" alwaysNotify="true" />
    </interface>

    <script type="text/brightscript" uri="pkg:/components/HomeView.brs" />
    <script type="text/brightscript" uri="pkg:/source/Utils.brs" />
    <script type="text/brightscript" uri="pkg:/source/Watchlist.brs" />

    <children>
        <!-- 2 rows visible. itemSize.y = poster 380 + label headroom 80
             (LargeBoldSystemFont is ~50px tall; 80 leaves a small gutter
             below for visual breathing room). floatingFocus keeps labels
             glued to their rows as the user scrolls. -->
        <RowList id="rows"
                 translation="[60, 20]"
                 itemSize="[1800, 460]"
                 numRows="2"
                 rowItemSize="[[220, 380]]"
                 rowItemSpacing="[[20, 0]]"
                 rowSpacings="[20]"
                 rowFocusAnimationStyle="floatingFocus"
                 showRowLabel="[true]"
                 showRowCounter="[false]"
                 rowLabelOffset="[[0, 0]]"
                 rowLabelFont="font:LargeBoldSystemFont"
                 rowLabelColor="0xffffffff"
                 focusXOffset="0" />

        <Label id="empty" translation="[60, 400]" text="No content. Pull-to-refresh by pressing OK."
               font="font:MediumSystemFont" color="0x808080ff" visible="false" />
    </children>
</component>
